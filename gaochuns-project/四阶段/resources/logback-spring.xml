<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">


    <!-- 日志的格式
       %d          表示日期
       %thread     表示线程名
       %-5level    日志级别，从左显示5个字符宽度
       %logger{56} 日志打印所属的类名，限定长度56个字符
       %msg        日志消息
       %n          是换行符
       颜色设置    %颜色（以上参数），例如，%highlight(%-5level)
       支持颜色    "%black", "%red", "%green","%yellow","%blue", "%magenta","%cyan", "%white", "%gray", "%boldRed",
                   "%boldGreen", "%boldYellow", "%boldBlue", "%boldMagenta""%boldCyan", "%boldWhite" and "%highlight"
   -->


    <!-- 控制台输出 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%boldYellow(%date{yyyy-MM-dd HH:mm:ss}) |%highlight(%-5level)  |%boldGreen(%logger:%line) |%boldWhite(%msg%n)</pattern>

        </encoder>
    </appender>

    <!-- 日志输出级别 -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!--打印SQL-->
    <logger name="com.gaochun.mapper" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT"/>
    </logger>
</configuration>